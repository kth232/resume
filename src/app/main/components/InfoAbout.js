import React from 'react';
import { Section } from 'react-fullpage';

const InfoAbout = () => {
  return (
    <Section>
      <h1>INFO & ABOUT</h1>
    </Section>
  );
};

export default React.memo(InfoAbout);
